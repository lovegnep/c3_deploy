











x700026_CSP_FUBENTYPE				= 	0
x700026_CSP_SCRIPTID				=	1
x700026_CSP_TICKCOUNT				= 	2
x700026_CSP_FROMSCENEID				= 	3
x700026_CSP_ISCLOSING				= 	4
x700026_CSP_LEAVECOUNTDOWN			= 	5
x700026_CSP_TEAMID					=	6
x700026_CSP_BACKSCENEX       		=   7
x700026_CSP_BACKSCENEZ      		=   8
x700026_CSP_FUBENLEVEL			=	9
x700026_CSP_KILLCOUNT				=	10
x700026_CSP_FUBEN_SCENE_ID			=	11
x700026_CSP_GAME_STEP				=	12
x700026_CSP_STEP_TICKCOUNT			=	13
x700026_CSP_NPC_ID					=	14
x700026_CSP_SCORE					=	15
x700026_CSP_KILLCOUNT				=	16
x700026_CSP_NEEDKILLCOUNT			=	17


x700026_CSP_RESULT_START			=	20

x700026_CSP_GP_BOXID_START			=	30
x700026_CSP_GP_ID_START				=	65

x700026_CSP_GP_POSIDX_START			=	200
x700026_CSP_HUMAN_COUNT				=	240
x700026_CSP_OBJID_START				=	241
x700026_CSP_GUID_START              =   248



x700026_ENTER_TYPE_TEAM			=	1
x700026_ENTER_TYPE_FUQI			=	2
x700026_ENTER_TYPE_BANGHUI		=	3
x700026_FubenEnterType			=	x700026_ENTER_TYPE_TEAM



x700026_var_FileId 					= 700026
x700026_var_FubenName				= "【队伍】勾魂牌局"
x700026_var_SceneMapNavPath			= 	"fuben_fututabaceng/fuben_fututabaceng.nav"
x700026_var_NoUserTime				= 	30						
x700026_var_TickTime					= 	5						
x700026_var_FubenType				= 	FUBEN_FOSHOU		    
x700026_var_LimitTotalHoldTime		= 	540						
x700026_var_TimeZoneBias				= 	0;
x700026_var_CloseTick					= 	6						
x700026_var_NoUserTime				= 	30						
x700026_var_DeadTrans					= 	0						
x700026_var_Fuben_X					= 	21						
x700026_var_Fuben_Z					= 	20						
x700026_var_Back_X					= 	310						
x700026_var_Back_Z					= 	36						
x700026_var_BackScene					=	{0,0,0,0}


x700026_var_GPInterval		=	1000
x700026_var_GPInterval2		=	10000
x700026_var_IntervalList = {2,2*12,2*12,2*12,6}


x700026_var_GrowPosLen		=	56
x700026_var_GrowPoint_PosList = {
							{varX=26,z=63},
							{varX=26,z=61},
							{varX=26,z=59},
							{varX=26,z=57},
							{varX=26,z=65},
							{varX=26,z=67},
							{varX=26,z=69},
							{varX=24,z=63},
							{varX=24,z=61},
							{varX=24,z=59},
							{varX=24,z=57},
							{varX=24,z=65},
							{varX=24,z=67},
							{varX=24,z=69},
							{varX=22,z=63},
							{varX=22,z=61},
							{varX=22,z=59},
							{varX=22,z=57},
							{varX=22,z=65},
							{varX=22,z=67},
							{varX=22,z=69},
							{varX=28,z=63},
							{varX=28,z=61},
							{varX=28,z=59},
							{varX=28,z=57},
							{varX=28,z=65},
							{varX=28,z=67},
							{varX=28,z=69},
							{varX=30,z=63},
							{varX=30,z=61},
							{varX=30,z=59},
							{varX=30,z=57},
							{varX=30,z=65},
							{varX=30,z=67},
                            {varX=30,z=69},
							{varX=32,z=63},
                            {varX=32,z=61},
                            {varX=32,z=59},
                            {varX=32,z=57},
                            {varX=32,z=65},
                            {varX=32,z=67},
                            {varX=32,z=69},
							{varX=34,z=63},
                            {varX=34,z=61},
                            {varX=34,z=59},
                            {varX=34,z=57},
                            {varX=34,z=65},
                            {varX=34,z=67},
                            {varX=34,z=69},
							{varX=20,z=63},
                            {varX=20,z=61},
                            {varX=20,z=59},
                            {varX=20,z=57},
                            {varX=20,z=65},
                            {varX=20,z=67},
                            {varX=20,z=69},
						}

x700026_var_Pai_NameList	=	{
								{varId = 1	,varName="1条"},
								{varId = 2	,varName="2条"},
								{varId = 3	,varName="3条"},
								{varId = 4	,varName="4条"},
								{varId = 5	,varName="5条"},
								{varId = 6	,varName="6条"},
								{varId = 7	,varName="7条"},
								{varId = 8	,varName="8条"},
								{varId = 9	,varName="9条"},
								{varId = 10,varName="1筒"},
								{varId = 11,varName="2筒"},
								{varId = 12,varName="3筒"},
								{varId = 13,varName="4筒"},
								{varId = 14,varName="5筒"},
								{varId = 15,varName="6筒"},
								{varId = 16,varName="7筒"},
								{varId = 17,varName="8筒"},
								{varId = 18,varName="9筒"},
								{varId = 19,varName="1万"},
								{varId = 20,varName="2万"},
								{varId = 21,varName="3万"},
								{varId = 22,varName="4万"},
								{varId = 23,varName="5万"},
								{varId = 24,varName="6万"},
								{varId = 25,varName="7万"},
								{varId = 26,varName="8万"},
								{varId = 27,varName="9万"},
								{varId = 28,varName="中"},
								{varId = 29,varName="发"},
								{varId = 30,varName="白"},
								{varId = 31,varName="东"},
								{varId = 32,varName="南"},
								{varId = 33,varName="西"},
								{varId = 34,varName="北"}
							}


x700026_var_Start_GPID		=	500

x700026_var_Buff1				=	14110


x700026_var_DayCountUntil				=	10

x700026_var_LimitMembers				=	1


x700026_var_LevelMin					=   100
x700026_var_LevelMax					=   100

x700026_NeedKillMonster_Count		=	1

x700026_var_EnterTime					=	{                       
											{min=0*60+0,  max=24*60+0},
										}

x700026_var_EnterDay					=	{0,1,2,3,4,5,6};




x700026_var_EnterInfo					=	"\t银钩赌坊内的勾魂牌局是蓝胡子和赵金手共同设下的圈套，专门引诱江湖上阅历尚浅的少年侠士进入牌局，将他们所有的钱财骗光。明教前教主玉罗刹之子玉柱也被困其中，要救出玉柱等人，必须先破了蓝胡子和赵金手的勾魂牌局。\n#Y开放时间：#W全天\n#Y友情提示：#W组上几个志同道合的朋友，可以提高效率！\n#Y副本说明：#W在这个副本中，你可以获得意外的奖励！"
x700026_var_FuBenCreateOK				= 	"副本创建完成！"
x700026_var_FuBenCreateFailed			= 	"副本已达上限，请稍后再试！"
x700026_var_CountDownMessage			= 	"你将在%d秒后离开场景！"
x700026_var_TimeOutFailedMessage		= 	"副本时间到！即将被传出副本！"

x700026_str_NotTime				= "进入副本的时间未到"
x700026_str_UnderLevel			= format("你的等级不足%d级！", x700026_var_LevelMin)
x700026_str_UpLevel				= "等级过高"
x700026_str_TimesOver			= "超出当日次数限制"	
x700026_str_NotDay				= "今天不能进入副本"


x700026_var_EnterPos                  =   {
                                            { idx = 1, varX = 33, z=58 },
                                            { idx = 2, varX = 31, z=58 },
                                            { idx = 3, varX = 36, z=58 },
                                            { idx = 4, varX = 33, z=61 },
                                            { idx = 5, varX = 31, z=61 },
                                            { idx = 6, varX = 36, z=61 },
                                        }

x700026_var_BackPos                  =   {
                                            { idx = 1, varX = 259, z=272 },
                                            { idx = 2, varX = 259, z=272 },
                                            { idx = 3, varX = 259, z=272 },
                                            { idx = 4, varX = 259, z=272 },
                                            { idx = 5, varX = 259, z=272 },
                                            { idx = 6, varX = 259, z=272 },
                                        }

x700026_var_SubmitNPC                 =   { type =25037,guid= 150537,varX = 32, z=60, facedir =0, title="" }


x700026_var_GameNPC                 =   { type =25036,guid= 150538,varX = 36, z=56, facedir=0, title="" }



x700026_var_BossCreate				=	{
                                            {levelmin =60, levelmax=64, varName ="通灵筛子",   type= 26861,  varX=37, z=59,  r=2, ai=7, aiscript=-1, facedir = 0, title = ""},
                                            {levelmin =65, levelmax=69, varName ="通灵筛子",   type= 26862,  varX=37, z=59,  r=2, ai=7, aiscript=-1, facedir = 0, title = ""},
                                            {levelmin =70, levelmax=74, varName ="通灵筛子",   type= 26863,  varX=37, z=59,  r=2, ai=7, aiscript=-1, facedir = 0, title = ""},
                                            {levelmin =75, levelmax=79, varName ="通灵筛子",   type= 26864,  varX=37, z=59,  r=2, ai=7, aiscript=-1, facedir = 0, title = ""},
                                            {levelmin =80, levelmax=84, varName ="通灵筛子",   type= 26865,  varX=37, z=59,  r=2, ai=7, aiscript=-1, facedir = 0, title = ""},
                                            {levelmin =85, levelmax=89, varName ="通灵筛子",   type= 26866,  varX=37, z=59,  r=2, ai=7, aiscript=-1, facedir = 0, title = ""},
                                            {levelmin =90, levelmax=94, varName ="通灵筛子",   type= 26867,  varX=37, z=59,  r=2, ai=7, aiscript=-1, facedir = 0, title = ""},
                                            {levelmin =95, levelmax=100, varName ="通灵筛子",  type= 26868,  varX=37, z=59,  r=2, ai=7, aiscript=-1, facedir = 0, title = ""},

										

										}






function x700026_ProcEnumEvent(varMap, varPlayer, varTalknpc, varQuest)
	
	local varLevel = GetLevel( varMap,varPlayer )
    if varLevel < x700026_var_LevelMin then
       return
    end

    if varLevel > x700026_var_LevelMax then
		 return
    end
    
	TalkAppendButton(varMap, x700026_var_FileId, x700026_var_FubenName,14,1);
	
end




function x700026_DispatchEnterInfo( varMap, varPlayer, varTalknpc )

	StartTalkTask(varMap)

		
		TalkAppendString(varMap,"#Y"..x700026_var_FubenName)
		TalkAppendString(varMap,x700026_var_EnterInfo)
		

		


	StopTalkTask()
	DeliverTalkInfo(varMap, varPlayer, varTalknpc, x700026_var_FileId, -1);
end





function x700026_ProcAcceptCheck( varMap, varPlayer, varTalknpc )
    return 1
end




function x700026_ProcAccept( varMap, varPlayer )

	-- x700026_ProcRequestEnterFuben(varMap, varPlayer);
end









function x700026_ProcEventEntry(varMap, varPlayer, varTalknpc,varScriptFileId, nFlag)

	if nFlag== nil then
		return
	end

	
	

	x700026_DispatchEnterInfo( varMap, varPlayer, varTalknpc )
	
end






function x700026_ProcRequestEnterFuben(varMap, varPlayer)
	local varRet = x700026_CheckRequest(varMap, varPlayer)


	if varRet>0 then
		x700026_ProcApproveRequestEnterFuben(varMap, varPlayer, varRet)
	end
end






function x700026_ProcFubenSceneCreated( varMap )
	
	
	
	local humancount = GetFuben_PlayerCount(varMap);
	SetFubenData_Param(varMap, x700026_CSP_HUMAN_COUNT, humancount);
	for	varI = 0, humancount - 1 do
		local humanId = GetFuben_PlayerObjId(varMap, varI);
		
	end
	
	SetFubenData_Param(varMap, x700026_CSP_GAME_STEP, 0);
	SetFubenData_Param(varMap, x700026_CSP_STEP_TICKCOUNT, 1);

	SetFubenData_Param(varMap, x700026_CSP_SCORE, 0);
	

	for varI=1, 34 do
		SetFubenData_Param(varMap, x700026_CSP_GP_ID_START+varI, -1);

		SetGrowPointIntervalContainer(varMap,x700026_var_Start_GPID+varI,-1)
	end



	x700026_CreateNpc( varMap)
		
end



function x700026_CheckRequest(varMap, varPlayer)

	local varRet = x700026_CheckCommonCondition(varMap, varPlayer)

	if varRet<=0 then
		return 0;
	end

	
	
	if x700026_FubenEnterType == x700026_ENTER_TYPE_TEAM then

		varRet =  x700026_CheckTeamCondition(varMap, varPlayer);
		if varRet>0 then
			return x700026_ENTER_TYPE_TEAM;
		end

	elseif x700026_FubenEnterType == x700026_ENTER_TYPE_FUQI then
		
		varRet =  x700026_CheckFuqiCondition(varMap, varPlayer);
		if varRet>0 then
			return x700026_ENTER_TYPE_FUQI;
		end

	elseif x700026_FubenEnterType == x700026_ENTER_TYPE_BANGHUI then
		
		varRet =  x700026_CheckBanghuiCondition(varMap, varPlayer);	
		if varRet>0 then
			return x700026_ENTER_TYPE_BANGHUI;
		end
	
	else
		
		return 0;
	end

	
	
	
	return 0;
end

function x700026_CheckCommonCondition(varMap, varPlayer)

	
	local varRet = x700026_CheckCommonCondition_Day(varMap, varPlayer)  

	if varRet <=0 then
		return 0;
	end
	
	local varRet = x700026_CheckCommonCondition_Time(varMap, varPlayer)  

	if varRet <=0 then
		return 0;
	end
	
	local varRet = x700026_CheckCommonCondition_Level(varMap, varPlayer)  

	if varRet <=0 then
		return 0;
	end

	local varRet = x700026_CheckCommonCondition_Times(varMap, varPlayer)  

	if varRet <=0 then
		return 0;
	end

	return 1;


	
end

function x700026_CheckCommonCondition_Time(varMap, varPlayer)
	local hour,minute,sec =GetHourMinSec();
	local nowtime = hour*60+minute
	
	for varI, item in x700026_var_EnterTime do
		if nowtime >= item.min and nowtime <= item.max then
			
			
			return 1;
			
		end
	end
	StartTalkTask(varMap);
		TalkAppendString(varMap, x700026_str_NotTime);
	StopTalkTask();
	DeliverTalkTips(varMap, varPlayer);
	return 0;
end

function x700026_CheckCommonCondition_Day(varMap, varPlayer)

	local weekDay = GetWeek()
	for varI, item in x700026_var_EnterDay do
		if weekDay==item then
			
			
			return 1;
			
		end
	end
	StartTalkTask(varMap);
		TalkAppendString(varMap, x700026_str_NotDay);
	StopTalkTask();
	DeliverTalkTips(varMap, varPlayer);
	return 0;
end

function x700026_CheckCommonCondition_Level(varMap, varPlayer)
	
	local varLevel = GetLevel( varMap,varPlayer )
    if varLevel < x700026_var_LevelMin then
        StartTalkTask(varMap);
			TalkAppendString(varMap, x700026_str_UnderLevel);
		StopTalkTask();
		DeliverTalkTips(varMap, varPlayer);
		return 0
    end

    if varLevel > x700026_var_LevelMax then
		 StartTalkTask(varMap);
			TalkAppendString(varMap, x700026_str_UpLevel);
		StopTalkTask();
		DeliverTalkTips(varMap, varPlayer);
        return 0
    end
	
	return 1;
end



function x700026_CheckCommonCondition_Times(varMap, varPlayer)
	if x700026_var_DayCountUntil >0 then
		local varDayCount = x700026_GetDayCount(varMap, varPlayer)
		if varDayCount>= x700026_var_DayCountUntil then
			StartTalkTask(varMap);
				TalkAppendString(varMap, x700026_str_TimesOver);
			StopTalkTask();
			DeliverTalkTips(varMap, varPlayer);
			return 0;
		end
	end
	return 1;
end



function x700026_CheckTeamCondition(varMap, varPlayer)
	local teamid = GetTeamId(varMap, varPlayer);
	local teamnum = GetTeamSize(varMap, varPlayer);
	local nearteamnum = GetNearTeamCount(varMap, varPlayer);

	local varStr = "";
	local bPassed = 1;

	if IsTeamLeader(varMap, varPlayer) <= 0 then
		varStr	= "你不是队长！"
		bPassed = 0
	elseif x700026_CheckAllMemberNear( varMap,varPlayer) <= 0 then
		varStr	= "需要所有的队友在附近！"
		bPassed = 0
	elseif teamid < 0 then
		varStr	= "你不在队伍中！"
		bPassed = 0
	elseif teamnum < x700026_var_LimitMembers then
		varStr	= format("你的队伍不足%d人！", x700026_var_LimitMembers)
		bPassed = 0
	elseif nearteamnum < x700026_var_LimitMembers then
		varStr	= format("附近队友不足%d人！", x700026_var_LimitMembers)
		bPassed = 0
	else

		local varRet, tips;

		varRet, tips = x700026_CheckAllMemberMinLevel(varMap, varPlayer, nearteamnum);
		if varRet <= 0 then
			varStr = tips;
			bPassed = 0
		end
		varRet, tips = x700026_CheckAllMemberMaxLevel( varMap, varPlayer, nearteamnum )

		if varRet <= 0 then
			varStr = tips;
			bPassed = 0
		end

		varRet, tips = x700026_CheckAllMemberDayCount(varMap, varPlayer, nearteamnum);
		if varRet == 0 then
			varStr = tips;
			bPassed = 0
		end



		 
		varRet, tips = x700026_CheckAllMemberErrorState(varMap, varPlayer, nearteamnum)
		if varRet <= 0 then
			varStr = tips;
			bPassed = 0
		end

		if varRet<=0 then
			varStr = tips;
			bPassed = 0
		end
		

	end

	if bPassed == 1 then
		return 1;
	else
		if varStr ~= "" then
			StartTalkTask(varMap);
				TalkAppendString(varMap, varStr);
			StopTalkTask();
			DeliverTalkTips(varMap, varPlayer);
			return 0;
		end
	end


	return 1;
end

function x700026_CheckFuqiCondition(varMap, varPlayer)
	return 1;
end

function x700026_CheckBanghuiCondition(varMap, varPlayer)
	return 1;
end







function x700026_ProcApproveRequestEnterFuben(varMap, varPlayer, nEnterType)
	
	
	if nEnterType == x700026_ENTER_TYPE_TEAM then

		local memberNum = GetNearTeamCount(varMap, varPlayer);

		x700026_MakeFuben_Team(varMap, varPlayer, memberNum);

	elseif nEnterType == x700026_ENTER_TYPE_FUQI then
		
		

	elseif nEnterType == x700026_ENTER_TYPE_BANGHUI then
		
		
	
	else
		
		return 0;
	end

	

end



function x700026_MakeFuben_Team(varMap, varPlayer, memberNum)
	
	for varI = 0, memberNum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local ErrorCode = x700026_IsPlayerErrorState( varMap, memberId )
        if ErrorCode == 0 then
            
            SetPlayerMutexState( varMap, memberId , PLAYER_STATE_READYFUBEN ,1)
        end
    end

	local leaderguid = ObjId2Guid(varMap, varPlayer);
    local varX,z =GetWorldPos(varMap,varPlayer)

	
	local sceneParamList ={}
	for varI=0, 255 do
		local nValue = GetFubenData_Param(varMap, varI);
		sceneParamList[varI+1] = nValue;
	end
	

	SetSceneLoad_Map(varMap, x700026_var_SceneMapNavPath); 								
	SetFubenData_TeamLeader(varMap, leaderguid);   
	SetFubenData_NoUserCloseTime(varMap, x700026_var_NoUserTime * 1000);            
	SetFubenData_Timer(varMap, x700026_var_TickTime * 1000);                        
	SetFubenData_Param(varMap, x700026_CSP_FUBENTYPE, x700026_var_FubenType);	
	SetFubenData_Param(varMap, x700026_CSP_SCRIPTID, x700026_var_FileId)			
	SetFubenData_Param(varMap, x700026_CSP_TICKCOUNT, 0);							
	SetFubenData_Param(varMap, x700026_CSP_FROMSCENEID, -1);						
	SetFubenData_Param(varMap, x700026_CSP_ISCLOSING, 0);							
	SetFubenData_Param(varMap, x700026_CSP_LEAVECOUNTDOWN, 0);					
    SetFubenData_Param(varMap, x700026_CSP_TEAMID, GetTeamId(varMap, varPlayer)); 	
	SetFubenData_Param(varMap, x700026_CSP_BACKSCENEX, varX) ;					    
	SetFubenData_Param(varMap, x700026_CSP_BACKSCENEZ, z);                        
	SetFubenData_Param(varMap, x700026_CSP_KILLCOUNT, 0);   

	
	for varI=x700026_CSP_GUID_START248, x700026_CSP_GUID_START+5 do
		SetFubenData_Param(varMap, varI ,0 )
	end
	



    for varI= 0 ,memberNum - 1  do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local guid = GetPlayerGUID( varMap,memberId )
        local paramidx = x700026_CSP_GUID_START + varI

        SetFubenData_Param(varMap, paramidx ,guid )
    end


	
    x700026_InitAutoLevel_Team(varMap,varPlayer, memberNum,2)
    

	local varSelectMap = CreateFuben(varMap); 										

	
	for varI=0, 255 do
		local nValue = sceneParamList[varI+1];
		SetFubenData_Param(varMap, varI,nValue);
	end
	


    if varSelectMap > 0 then
        for varI = 0, memberNum - 1 do
            local memberId = GetNearTeamMember(varMap, varPlayer, varI);
            StartTalkTask(varMap);
            TalkAppendString(varMap, x700026_var_FuBenCreateOK);
            StopTalkTask();
            DeliverTalkTips(varMap, memberId);
        end


        
		GamePlayScriptLog(varMap, varPlayer, 1201)
		

    else

        for varI = 0, memberNum - 1 do
            local memberId = GetNearTeamMember(varMap, varPlayer, varI);
            StartTalkTask(varMap);
            TalkAppendString(varMap, x700026_var_FuBenCreateFailed);
            StopTalkTask();
            DeliverTalkTips(varMap, memberId);

            
            SetPlayerMutexState( varMap,memberId ,PLAYER_STATE_READYFUBEN ,0)
        end
    end
end





function x700026_ProcFubenReady(varMap, destsceneId)
	
	if x700026_FubenEnterType == x700026_ENTER_TYPE_TEAM then

		x700026_ProcFubenReady_Team(varMap, destsceneId)

	elseif x700026_FubenEnterType == x700026_ENTER_TYPE_FUQI then
		
		x700026_ProcFubenReady_Fuqi(varMap, destsceneId)

	elseif x700026_FubenEnterType == x700026_ENTER_TYPE_BANGHUI then
		
		x700026_ProcFubenReady_Banghui(varMap, destsceneId)
	
	else
		
		return 0;
	end

	

end

function x700026_ProcFubenReady_Team(varMap, destsceneId)

	for varI=10, x700026_CSP_GUID_START do
		SetFubenData_Param(destsceneId,varI,0)
	end
	
	SetFubenData_Param(destsceneId, x700026_CSP_FUBEN_SCENE_ID, destsceneId);
	
	local leaderguid = GetFubenData_TeamLeader(destsceneId);
	local leaderObjId = Guid2ObjId(varMap, leaderguid);

	local	memberNum = GetNearTeamCount(varMap, leaderObjId);


	
    local varMsg = "CSD,EnterFuben,"..x700026_var_FileId..","..destsceneId..","..memberNum
	
	
	local j=0
	for varI=x700026_CSP_GUID_START, x700026_CSP_GUID_START+5 do
		local nGuid = GetFubenData_Param(destsceneId,varI)

		

		if nGuid~=0 then
			local memberId =  Guid2ObjId(varMap, nGuid);

			local nFromSceneId = GetFubenData_Param(destsceneId, x700026_CSP_FROMSCENEID)
			if nFromSceneId<0 then
				if IsPlayerStateNormal(varMap,memberId ) > 0 then
					local countryId = GetCurCountry( varMap, memberId )
					SetFubenData_Param(destsceneId, x700026_CSP_FROMSCENEID, x700026_var_BackScene[countryId+1]);
				end
			end

			

			local ErrorCode = x700026_IsPlayerErrorState( varMap,memberId )
			if ErrorCode == 0 then 
				
				
				local varX = x700026_var_EnterPos[j+1].varX
				local z = x700026_var_EnterPos[j+1].z
				if LuaCallNoclosure(COUNTRY_BATTLE_LITE_SCRIPT,"CheckCountryBattleLiteSceneTrans",varMap,memberId,destsceneId) == 1 then
					NewWorld(varMap, memberId, destsceneId, varX, z, 700026);
					varMsg = varMsg..","..nGuid
				end
				
				
				j = j+1
			end

			if IsPlayerStateNormal(varMap,memberId ) > 0 then
				
				SetPlayerMutexState( varMap,memberId ,PLAYER_STATE_READYFUBEN ,0)
			end

			
		end
	end
	

	
    WriteLog(1,varMsg)

end

function x700026_GetDayCountUntil(varMap, varPlayer)
	return x700026_var_DayCountUntil;
end

function x700026_GetBackPosition(varMap, varPlayer)
	local npos = random(1,6)
	local x1 = x700026_var_BackPos[npos].varX
	local z1 = x700026_var_BackPos[npos].z

	return x1,z1
	
end

function x700026_ProcFubenReady_Fuqi(varMap, destsceneId)
end

function x700026_ProcFubenReady_Banghui(varMap, destsceneId)
end




function x700026_ProcPlayerEnter(varMap, varPlayer)
	
	local leaveFlag = GetFubenData_Param(varMap, x700026_CSP_ISCLOSING);

	if leaveFlag > 0 then

		
		local leaveCountDown = GetFubenData_Param(varMap, x700026_CSP_LEAVECOUNTDOWN) + 1;
		
		if leaveCountDown >= x700026_var_CloseTick then
			local oldsceneId = GetFubenData_Param(varMap, x700026_CSP_FROMSCENEID);
			
			RelivePlayerNM( varMap,varPlayer,2) 
			SetPlayerMutexState( varMap,varPlayer,PLAYER_STATE_DIE,0)
			TimerCommand( varMap, varPlayer, 1, 7010, 3, x700026_var_FileId, -1, -1 );


			local npos = random(1,6)
			local varX = x700026_var_BackPos[npos].varX
			local z = x700026_var_BackPos[npos].z

			NewWorld(varMap, varPlayer, oldsceneId, varX, z, 700026);
			return
		end
			
	end
	

	SetPlayerDefaultReliveInfo(varMap, varPlayer, "%10", "%10", "%10", varMap, x700026_var_Fuben_X, x700026_var_Fuben_Z);

	
	local myGuid = GetPlayerGUID( varMap,varPlayer )
	myGuid = format("%u",myGuid)
	local bFind =0
	for varI= 0 ,6 - 1  do
       
        local paramidx = x700026_CSP_GUID_START + varI

        local guid = GetFubenData_Param(varMap, paramidx )
		guid=format("%u",guid)
		if myGuid==guid then
			bFind =1
			break;
		end
    end

	if bFind==0 then
		local varDayCount = x700026_GetDayCount(varMap, varPlayer)
		FubenIllegalEnterLog(varMap, varPlayer, 1201,varDayCount)
		
	end
	
	
	
	
end

function x700026_ProcPlayerEnterFinished(varMap, varPlayer)
	
end



function x700026_ProcEnterTick(varMap,nowTickCount)

	local varCurrentStep = GetFubenData_Param(varMap, x700026_CSP_GAME_STEP);
	local nTickCount = GetFubenData_Param(varMap, x700026_CSP_STEP_TICKCOUNT) + 1;
	SetFubenData_Param(varMap, x700026_CSP_STEP_TICKCOUNT, nTickCount);

	
	if nTickCount == x700026_var_IntervalList[varCurrentStep+1] then
		if varCurrentStep<4 then
			if varCurrentStep+1>1 then
				local nNpcId = GetFubenData_Param(varMap, x700026_CSP_NPC_ID )
				NpcTalk(varMap, nNpcId, "时间到，本轮结束",  -1)
				x700026_ShowTipsToAll(varMap, "时间到，本轮结束")
			end
			x700026_StartStep(varMap, varCurrentStep+1)
		elseif varCurrentStep==4 then
			x700026_ProcAllGatherCompleted(varMap)
		end
	else
		if varCurrentStep>=1 and varCurrentStep<=4 then
			
			for varI=1, 34 do
				local nGrowpointId = GetFubenData_Param(varMap, x700026_CSP_GP_ID_START+varI);
				local nStatus = GetFubenData_Param(varMap, x700026_CSP_GP_BOXID_START+varI);
				if nGrowpointId<0 and nStatus==-2 then
					 SetFubenData_Param(varMap, x700026_CSP_GP_BOXID_START+varI,-1);

					 x700026_InitMyGrowXZ(varMap, varI)

					 local nPosIndex = GetFubenData_Param(varMap, x700026_CSP_GP_POSIDX_START+varI)
					 local varX = x700026_var_GrowPoint_PosList[nPosIndex].varX
					 local z = x700026_var_GrowPoint_PosList[nPosIndex].z

					 SetGrowPointPos(varMap,x700026_var_Start_GPID+varI,varX,z)
				end
			end
		end
	end

	

	

	
end

function x700026_StartStep(varMap, nNextStep)

	
	
	if nNextStep<1 or nNextStep>4 then
		return
	end

	local varCurrentStep = GetFubenData_Param(varMap, x700026_CSP_GAME_STEP);

	if varCurrentStep~=nNextStep-1 then
		return
	end

	SetFubenData_Param(varMap, x700026_CSP_GAME_STEP,nNextStep);
	SetFubenData_Param(varMap, x700026_CSP_STEP_TICKCOUNT, 0);

	x700026_DeleteAllGrowPoint(varMap)

	x700026_InitGrowXZ(varMap)

	for varI=1, 34 do

		local nPosIndex = GetFubenData_Param(varMap, x700026_CSP_GP_POSIDX_START+varI)
		local varX = x700026_var_GrowPoint_PosList[nPosIndex].varX
		local z = x700026_var_GrowPoint_PosList[nPosIndex].z
		
		SetGrowPointPos(varMap,x700026_var_Start_GPID+varI,varX,z)
	end
	
	for varI=1, 34 do
		SetGrowPointIntervalContainer(varMap,x700026_var_Start_GPID+varI,x700026_var_GPInterval)
		
		
		
		
		
		
		
	end
	
	local nNpcId = GetFubenData_Param(varMap, x700026_CSP_NPC_ID )
	if nNextStep==1 then
		x700026_ShowTipsToAll(varMap, "第一轮开始")
		local iResult = random(1,3)

		if iResult ==1 then
			NpcTalk(varMap, nNpcId, "采集所有的条子",  -1)
			x700026_ShowTipsToAll(varMap, "采集所有的条子")	
		elseif iResult ==2 then
			NpcTalk(varMap, nNpcId, "采集所有的筒子",  -1)
			x700026_ShowTipsToAll(varMap, "采集所有的筒子")	
		elseif iResult ==3 then
			NpcTalk(varMap, nNpcId, "采集所有的万子",  -1)
			x700026_ShowTipsToAll(varMap, "采集所有的万子")	
		else
			return
		end
		
		SetFubenData_Param(varMap, x700026_CSP_RESULT_START,iResult);
	elseif nNextStep==2 then
		x700026_ShowTipsToAll(varMap, "第一轮结束，第二轮开始")
		NpcTalk(varMap, nNpcId, "采集所有的风牌和箭牌",  -1)
		x700026_ShowTipsToAll(varMap, "采集所有的风牌和箭牌")	
	elseif nNextStep==3 then

		x700026_ShowTipsToAll(varMap, "第二轮结束，第三轮开始")
			

		local iSeg = random(1,3)
		local iResult = random((iSeg-1)*9+1, (iSeg-1)*9+7)

		
		
		local varStr = x700026_GetShunziName(varMap,iResult)
		varStr = "采集顺子"..varStr
		NpcTalk(varMap, nNpcId, varStr,  -1)
		x700026_ShowTipsToAll(varMap, varStr)

		SetFubenData_Param(varMap, x700026_CSP_RESULT_START,iResult);
	elseif nNextStep==4 then
		x700026_ShowTipsToAll(varMap, "第三轮结束，第四轮开始")
		NpcTalk(varMap, nNpcId, "抢积分时间到！牌面越大积分越多,采集时间30秒",  -1)
		x700026_ShowTipsToAll(varMap, "抢积分时间到！牌面越大积分越多,采集时间30秒")	
	end

	
	
end

function x700026_GetShunziName(varMap,iStartId)

	local strTypeName = "";
	if iStartId<9 then
		strTypeName =format("%d,%d,%d条",iStartId,iStartId+1,iStartId+2);
	elseif iStartId<18 then
		strTypeName =format("%d,%d,%d筒",iStartId-9,iStartId-9+1,iStartId-9+2);
	elseif iStartId<27 then
		strTypeName =format("%d,%d,%d万",iStartId-18,iStartId-18+1,iStartId-18+2);
	end

	return strTypeName;



end

function x700026_ProcAnwer(varMap,nGrowpointId)
	local nAnswerId = nGrowpointId - x700026_var_Start_GPID
	local varCurrentStep = GetFubenData_Param(varMap, x700026_CSP_GAME_STEP);

	if varCurrentStep==1 then
		local iResult = GetFubenData_Param(varMap, x700026_CSP_RESULT_START)
		if nAnswerId>(iResult-1)*9 and nAnswerId<=iResult*9 then
			x700026_ProcCorrect(varMap,nAnswerId)
		else
			x700026_ProcIncorrect(varMap,nAnswerId)
		end
	elseif  varCurrentStep==2 then
		if nAnswerId<=27 then
			x700026_ProcIncorrect(varMap,nAnswerId)
		else
			x700026_ProcCorrect(varMap,nAnswerId)
		end
	elseif  varCurrentStep==3 then
		local iResult = GetFubenData_Param(varMap, x700026_CSP_RESULT_START)
		
		local bCorrect = 0
		for varI=iResult, iResult+2 do
			if nAnswerId==varI then
				bCorrect = 1
				break
			end
		end

		if bCorrect<=0 then
			x700026_ProcIncorrect(varMap,nAnswerId)
		else
			x700026_ProcCorrect(varMap,nAnswerId)
		end
	elseif varCurrentStep==4 then
		local nScore = GetFubenData_Param(varMap, x700026_CSP_SCORE);
		local nAdd =0
		if nAnswerId<=27 then
			if mod(nAnswerId,9)==0 then
				
				nAdd =9
			else
				nAdd = mod(nAnswerId,9)
			end
		else
			nAdd = 1
		end
		
		nScore	= nScore + nAdd;
		SetFubenData_Param(varMap, x700026_CSP_SCORE,nScore)
		x700026_ShowTipsToAll(varMap, format("回答正确，积分增加%d,总分为%d",nAdd,nScore))
	end

	

	

	


end

function x700026_ProcAllGatherCompleted(varMap)
	local varCurrentStep = GetFubenData_Param(varMap, x700026_CSP_GAME_STEP);
	if varCurrentStep~=4 then
		return
	end

	SetFubenData_Param(varMap, x700026_CSP_GAME_STEP,5);


	x700026_DeleteAllGrowPoint(varMap)
	


	

	local nScore = GetFubenData_Param(varMap, x700026_CSP_SCORE)
	local nNpcId = GetFubenData_Param(varMap, x700026_CSP_NPC_ID )

	NpcTalk(varMap, nNpcId, "时间到，本轮结束",  -1)
	x700026_ShowTipsToAll(varMap, format("选牌结束，你们总共得到%d分",nScore))
	x700026_ProcCreateFinalBoss(varMap)
end

function x700026_DeleteAllGrowPoint(varMap)
	for varI=1, 34 do
		local nGrowpointId = x700026_var_Start_GPID+varI

		
		RecycleGrowPointByType(varMap,nGrowpointId,-1)
		
		
		SetFubenData_Param(varMap, x700026_CSP_GP_ID_START+varI,-1);
			
		
	end
end

function x700026_ProcCreateFinalBoss(varMap)
	local nNpcId = GetFubenData_Param(varMap, x700026_CSP_NPC_ID )

	NpcTalk(varMap, nNpcId, "打骰子啦！",  -1)


	local fubenlevel = GetFubenData_Param(varMap, x700026_CSP_FUBENLEVEL ) ;

	
	local nScore = GetFubenData_Param(varMap, x700026_CSP_SCORE)


	local varCount =0;
	if nScore<=126 then
		varCount =1;
	elseif nScore<=252 then
		varCount =2;
	elseif nScore<=378 then
		varCount =4;
	elseif nScore<=480 then
		varCount =8;
	elseif nScore<=582 then
		varCount =10;
	elseif nScore<=683 then
		varCount =12;
	else
		varCount =18;
	end
	
	
	for varI, item in x700026_var_BossCreate do

        if item.levelmin <= fubenlevel and item.levelmax >= fubenlevel then
		   for j=0,varCount -1 do

                local rx = random(-item.r,item.r)
                local varX = item.varX + rx
                local rz = random(-item.r,item.r)
                local z = item.z + rz

				if item.title~="" then
					CreateMonster(varMap, item.type, varX, z, item.ai, item.aiscript, x700026_var_FileId, -1, 21,-1,item.facedir,  "",item.title)
				else
					CreateMonster(varMap, item.type, varX, z, item.ai, item.aiscript, x700026_var_FileId, -1, 21,-1,item.facedir)
				end
			end

        end
	end

	x700026_ShowTipsToAll(varMap, format("刷出%d个骰子！",varCount))

	SetFubenData_Param(varMap, x700026_CSP_KILLCOUNT, 0);
	SetFubenData_Param(varMap, x700026_CSP_NEEDKILLCOUNT, varCount);
end

function x700026_ProcDie(varMap, varPlayer, varKiller)
	local nDieCount = GetFubenData_Param(varMap, x700026_CSP_KILLCOUNT)
	SetFubenData_Param(varMap, x700026_CSP_KILLCOUNT, nDieCount+1);

	nDieCount = nDieCount+1;

	local nNeedKillCount = GetFubenData_Param(varMap, x700026_CSP_NEEDKILLCOUNT);

	if nNeedKillCount== nDieCount then

		
	
		local nowTickCount = GetFubenData_Param(varMap, x700026_CSP_TICKCOUNT)

		local nowTime = x700026_var_TickTime* nowTickCount;

		local nMinute = floor(nowTime/60)
		local nSec = mod(nowTime,60)

			
		local strPlayerName = GetName(varMap, varKiller)     
		
		local nKillerType = GetObjType(varMap, varKiller)
		
		if nKillerType== 3 then
			local nOwnerId = GetOwnerID(varMap, varKiller)
			strPlayerName = GetName(varMap, nOwnerId)
		end
		
		local strBossName = GetName(varMap, varPlayer)		 
		local strKillTime = format("%d分%d秒", nMinute, nSec) 

		
		
			LuaAllScenceM2Wrold (varMap,"#R四撇眉毛#G"..strPlayerName.."#R和他的队友们以钢铁的意志和超人的勇气仅用了#G"..strKillTime.."#R就顺利地救出了被困的玉柱。", 0, 1)
		

	
	
	
		x700026_ProcAllMonsterDead( varMap)
	end
end

function x700026_ProcAllMonsterDead( varMap)
	
	local nNpcId = GetFubenData_Param(varMap, x700026_CSP_NPC_ID )

	NpcTalk(varMap, nNpcId, "你们出色地完成了副本，可以离开了",  -1)

	x700026_CreateSubmitNpc( varMap)
end

function x700026_CreateSubmitNpc( varMap)
	if x700026_var_SubmitNPC.title~="" then
		CreateMonster(varMap, x700026_var_SubmitNPC.type, x700026_var_SubmitNPC.varX, x700026_var_SubmitNPC.z, 3, 0, -1, x700026_var_SubmitNPC.guid, -1,-1,x700026_var_SubmitNPC.facedir, "",x700026_var_SubmitNPC.title )
	else
		CreateMonster(varMap, x700026_var_SubmitNPC.type, x700026_var_SubmitNPC.varX, x700026_var_SubmitNPC.z, 3, 0, -1, x700026_var_SubmitNPC.guid, -1,-1,x700026_var_SubmitNPC.facedir)
	end
end


function x700026_ProcCorrect(varMap,nAnswerId)
	local nScore = GetFubenData_Param(varMap, x700026_CSP_SCORE)+1;
	SetFubenData_Param(varMap, x700026_CSP_SCORE,nScore)
	x700026_ShowTipsToAll(varMap, format("回答正确，积分增加1,总分为%d",nScore))

	













































end

function x700026_ProcIncorrect(varMap,nAnswerId)
	
	x700026_ShowTipsToAll(varMap, "回答错误")

	local humancount = GetFuben_PlayerCount(varMap);

	for varI = 0, humancount - 1 do


		local humanId = GetFuben_PlayerObjId(varMap, varI);	
		SendSpecificImpactToUnit(varMap, humanId,humanId,humanId, x700026_var_Buff1,0 )
		
	end
end


function x700026_GetGaterHoldTime(varMap)
	local varCurrentStep = GetFubenData_Param(varMap, x700026_CSP_GAME_STEP);
	local nTickCount = GetFubenData_Param(varMap, x700026_CSP_STEP_TICKCOUNT);

	if varCurrentStep>4 or varCurrentStep<1 then
		return -1;
	end

	local nHoldTime =0;

	
	nHoldTime = x700026_var_IntervalList[varCurrentStep+1]-nTickCount
	
	return nHoldTime;
end



function x700026_ProcFubenTimer(varMap, nowTime)

	
	
	
	local nowTickCount = GetFubenData_Param(varMap, x700026_CSP_TICKCOUNT) + 1;
	SetFubenData_Param(varMap, x700026_CSP_TICKCOUNT, nowTickCount);
	


	
	local leaveFlag = GetFubenData_Param(varMap, x700026_CSP_ISCLOSING);

	if leaveFlag > 0 then

		
		local leaveCountDown = GetFubenData_Param(varMap, x700026_CSP_LEAVECOUNTDOWN) + 1;
		SetFubenData_Param(varMap, x700026_CSP_LEAVECOUNTDOWN, leaveCountDown);

		if leaveCountDown >= x700026_var_CloseTick then
			local oldsceneId = GetFubenData_Param(varMap, x700026_CSP_FROMSCENEID);
			local humancount = GetFuben_PlayerCount(varMap);
			for	varI = 0, humancount - 1 do
				local humanId = GetFuben_PlayerObjId(varMap, varI);
                RelivePlayerNM( varMap,humanId,2) 
                SetPlayerMutexState( varMap,humanId,PLAYER_STATE_DIE,0) 


                TimerCommand( varMap, humanId, 1, 7010, 3, x700026_var_FileId, -1, -1 );

                local npos = random(1,6)
                local varX = x700026_var_BackPos[npos].varX
                local z = x700026_var_BackPos[npos].z

				NewWorld(varMap, humanId, oldsceneId, varX, z, 700026);
			end
		elseif leaveCountDown < x700026_var_CloseTick then
			local humancount = GetFuben_PlayerCount(varMap);
			for	varI = 0, humancount - 1 do
				local humanId = GetFuben_PlayerObjId(varMap, varI);
 				local varStr = format(x700026_var_CountDownMessage, (x700026_var_CloseTick - leaveCountDown) * x700026_var_TickTime);
  				StartTalkTask(varMap);
  				TalkAppendString(varMap, varStr);
  				StopTalkTask(varMap);

  				DeliverTalkTips(varMap, humanId);
			end
		end

	else

        if nowTickCount == 1 then
			
			x700026_ProcFubenSceneCreated(varMap )
           
        end

		


		
        

		local varCurrentStep = GetFubenData_Param(varMap, x700026_CSP_GAME_STEP);
		local bNormalTime = 0
		if varCurrentStep<1 or varCurrentStep>4 then
			bNormalTime = 1
		end

		

		local humancount = GetFuben_PlayerCount(varMap);

		if nowTickCount >= 2 then
            if humancount>0 then
				x700026_ProcEnterTick(varMap,nowTickCount);
			end
        end

		local nTime = 0;
		local nGatherHoldTime = x700026_GetGaterHoldTime(varMap)
		if bNormalTime==0 then
			nTime= nGatherHoldTime*5
		else
			nTime= (x700026_var_LimitTotalHoldTime - nowTickCount )*5
		end

		for varI = 0, humancount - 1 do
			local humanId = GetFuben_PlayerObjId(varMap, varI);
            
            TimerCommand( varMap, humanId, 1, 7010, 3, x700026_var_FileId, -1, -1 );

            if nTime > 0 then

                local nEventTime = nTime
                if nTime > 10 then
                    nEventTime = nTime - 10
                end
                
				if bNormalTime==0 then
					TimerCommand( varMap, humanId, 2, 7010, 3, x700026_var_FileId, nTime, nTime+10 );
				else
					TimerCommand( varMap, humanId, 2, 7010, 3, x700026_var_FileId, nTime, nTime );
				end
            end

			
			if nowTickCount == x700026_var_LimitTotalHoldTime then
				
				StartTalkTask(varMap);
					TalkAppendString(varMap, x700026_var_TimeOutFailedMessage);
				StopTalkTask();
				DeliverTalkTips(varMap, humanId);
			end
		end

        if nowTickCount == x700026_var_LimitTotalHoldTime then
            SetFubenData_Param(varMap, x700026_CSP_ISCLOSING,1);
        end
	end

end





function x700026_InitAutoLevel_Team( varMap,varPlayer ,memberNum,autoleveltype )

    if autoleveltype == 0 then          

        
        local maxlevel = 0
        for varI = 0, memberNum - 1 do
            local memberId = GetNearTeamMember(varMap, varPlayer, varI);
            local varLevel = GetLevel(varMap,memberId)

            if maxlevel < varLevel then
                maxlevel = varLevel
            end
        end

        SetFubenData_Param(varMap, x700026_CSP_FUBENLEVEL, maxlevel) ;					    

    elseif autoleveltype == 1 then      

        
        local minlevel = 1000
        for varI = 0, memberNum - 1 do
            local memberId = GetNearTeamMember(varMap, varPlayer, varI);
            local varLevel = GetLevel(varMap,memberId)

            if minlevel > varLevel then
                minlevel = varLevel
            end
        end

        SetFubenData_Param(varMap, x700026_CSP_FUBENLEVEL, minlevel) ;					    

    elseif autoleveltype == 2 then      

        

        local averagelevel = 0
        local levelval1 = 0
        local levelval2 = 0
        for varI = 0, memberNum - 1 do
            local memberId = GetNearTeamMember(varMap, varPlayer, varI);
            local varLevel = GetLevel(varMap,memberId)

            levelval1 = levelval1 + varLevel*varLevel
            levelval2 = levelval2 + varLevel
        end

        averagelevel = levelval1 / levelval2

        SetFubenData_Param(varMap, x700026_CSP_FUBENLEVEL, averagelevel) ;					

    end
end





function x700026_IsPlayerErrorState( varMap,varPlayer )

    local varState

    varState = IsPlayerStateNormal(varMap,varPlayer )
    if varState == 0 then
        return 1
    end

    varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_MENTALGAME )
    if varState == 1 then
        return 1
    end

    varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_STALL )
    if varState == 1 then
        return 1
    end

    varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_DIE )
    if varState == 1 then
        return 1
    end

    varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_CRASHBOX )
    if varState == 1 then
        return 1
    end

    varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_AUTOPLAY )
    if varState == 1 then
        return 1
    end

    varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_SUBTRAIN )
    if varState == 1 then
        return 1
    end

    varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_BUS )
    if varState == 1 then
        return 1
    end

		varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_ENJAIL )
    if varState == 1 then
        return 1
    end	

	
	varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_WEDDING )
    if varState == 1 then
        return 1
    end

	varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_TONGQUBUFF )
    if varState == 1 then
        return 1
    end

	varState = IsPlayerMutexState(varMap,varPlayer,PLAYER_STATE_TIESAN )
    if varState == 1 then
        return 1
    end
	


    return 0

end






function x700026_GetDayCount(varMap, varPlayer)
	local varToday = GetDayOfYear()
	local varLastday = GetPlayerGameData(varMap, varPlayer, MD_FUBEN_FOSHOU_DATE[1], MD_FUBEN_FOSHOU_DATE[2],MD_FUBEN_FOSHOU_DATE[3] );
	
	if varLastday ~= varToday then
		return 0
	end

	local varDaycount = GetPlayerGameData(varMap, varPlayer, MD_FUBEN_FOSHOU_DAYCOUNT[1], MD_FUBEN_FOSHOU_DAYCOUNT[2],MD_FUBEN_FOSHOU_DAYCOUNT[3] );
	return varDaycount;
end




function x700026_SetDayCount(varMap, varPlayer)

	local varToday = GetDayOfYear()

	local varLastday = GetPlayerGameData(varMap, varPlayer, MD_FUBEN_FOSHOU_DATE[1], MD_FUBEN_FOSHOU_DATE[2], MD_FUBEN_FOSHOU_DATE[3])

	if varLastday ~= varToday then
		SetPlayerGameData(varMap, varPlayer, MD_FUBEN_FOSHOU_DATE[1], MD_FUBEN_FOSHOU_DATE[2], MD_FUBEN_FOSHOU_DATE[3], varToday)
		SetPlayerGameData(varMap, varPlayer, MD_FUBEN_FOSHOU_DAYCOUNT[1], MD_FUBEN_FOSHOU_DAYCOUNT[2], MD_FUBEN_FOSHOU_DAYCOUNT[3], 1)
	else
		local varDaycount = GetPlayerGameData(varMap, varPlayer, MD_FUBEN_FOSHOU_DAYCOUNT[1], MD_FUBEN_FOSHOU_DAYCOUNT[2], MD_FUBEN_FOSHOU_DAYCOUNT[3])
		SetPlayerGameData(varMap, varPlayer, MD_FUBEN_FOSHOU_DAYCOUNT[1], MD_FUBEN_FOSHOU_DAYCOUNT[2], MD_FUBEN_FOSHOU_DAYCOUNT[3], varDaycount+1)
	end
	
	
	
end






function x700026_ProcPlayerDie(varMap, varPlayer, varKiller)
end






function x700026_CheckAllMemberNear(varMap, varPlayer)
    local nNearCount  = GetNearTeamCount( varMap,varPlayer )
    local nTotalCount = GetTeamSize( varMap,varPlayer )

    if nNearCount ~= nTotalCount then
        return 0
    end

    return 1
end






function x700026_CheckAllMemberMinLevel(varMap, varPlayer, teamnum)


	local tips,result

	result = 1

	if( x700026_var_LevelMin > 0 ) then
		for varI = 0, teamnum - 1 do
			local memberId = GetNearTeamMember(varMap, varPlayer, varI);
			if GetLevel(varMap, memberId) < x700026_var_LevelMin then
				if result == 1 then
					result = 0
					tips = GetName(varMap, memberId)
				else
					tips = tips..","..GetName(varMap, memberId)
				end
			end
		end
	end

	if result == 0 then
		tips = tips..format("的等级不足%d级！", x700026_var_LevelMin)
	end

	return result,tips;
end




function x700026_CheckAllMemberMaxLevel(varMap, varPlayer, teamnum)       


	local tips,result

	result = 1

	if( x700026_var_LevelMax > 0 ) then
		for varI = 0, teamnum - 1 do
			local memberId = GetNearTeamMember(varMap, varPlayer, varI);
			if GetLevel(varMap, memberId) > x700026_var_LevelMax then
				if result == 1 then
					result = 0
					tips = GetName(varMap, memberId)
				else
					tips = tips..","..GetName(varMap, memberId)
				end
			end
		end
	end

	if result == 0 then
		tips = tips.." 超出等级限制"
	end

	return result,tips;
end




function x700026_CheckAllMemberDayCount(varMap, varPlayer, teamnum)

	local tips,result

	result = 1

	if( x700026_var_DayCountUntil > 0 ) then
		for varI = 0, teamnum - 1 do
			local memberId = GetNearTeamMember(varMap, varPlayer, varI);
			if x700026_GetDayCount(varMap, memberId) >= x700026_var_DayCountUntil then
				if result == 1 then
					result = 0
					tips = GetName(varMap, memberId)
				else
					tips = tips..","..GetName(varMap, memberId)
				end
			end
		end
	end

	if result == 0 then
		tips = tips.." 超过当日进入副本次数限制"
	end

	return result,tips;

end






function x700026_CheckAllMemberErrorState( varMap, varPlayer, teamnum )

    local result,tips ;

    
    result,tips = x700026_CheckAllMemberErrorState_NormalState( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

    
    result,tips = x700026_CheckAllMemberErrorState_Die( varMap, varPlayer, teamnum )
    if result == 0 then
        return result,tips
    end

    
    result,tips = x700026_CheckAllMemberErrorState_AutoPlay( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

    
    result,tips = x700026_CheckAllMemberErrorState_Stall( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

    
    result,tips = x700026_CheckAllMemberErrorState_MentalGame( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

    
    result,tips = x700026_CheckAllMemberErrorState_CrashBox( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

    
    result,tips = x700026_CheckAllMemberErrorState_Bus( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

	 
    result,tips = x700026_CheckAllMemberErrorState_Enjail( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

	
	
	result,tips = x700026_CheckAllMemberErrorState_Wedding( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

	
	result,tips = x700026_CheckAllMemberErrorState_Tongqu( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

	
	result,tips = x700026_CheckAllMemberErrorState_Tiesan( varMap,varPlayer,teamnum )
    if result == 0 then
        return result,tips
    end

	

	
	




	
	


    return 1


end





function x700026_CheckAllMemberErrorState_NormalState( varMap, varPlayer, teamnum )
    local tips,result

	result = 1

    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerStateNormal(varMap,memberId )
        if varState == 0 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 离线或不在当前场景"
	end

	return result,tips;
end




function x700026_CheckAllMemberErrorState_MentalGame( varMap, varPlayer, teamnum )
    local tips,result

	result = 1

    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_MENTALGAME )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于答题状态"
	end

	return result,tips
end




function x700026_CheckAllMemberErrorState_Stall( varMap, varPlayer, teamnum )
    local tips,result

	result = 1

    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_STALL )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于设摊状态"
	end

	return result,tips
end




function x700026_CheckAllMemberErrorState_Die( varMap, varPlayer, teamnum )
    local tips,result

	result = 1

    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_DIE )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于死亡状态"
	end

	return result,tips
end




function x700026_CheckAllMemberErrorState_CrashBox( varMap, varPlayer, teamnum )
    local tips,result

	result = 1

    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_CRASHBOX )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于运镖状态"
	end

	return result,tips
end




function x700026_CheckAllMemberErrorState_AutoPlay( varMap, varPlayer, teamnum )
    local tips,result

	result = 1

    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_AUTOPLAY )
        local state2 = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_SUBTRAIN )
        if varState == 1 or state2 == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于代练或挂机状态"
	end

	return result,tips
end




function x700026_CheckAllMemberErrorState_Bus( varMap, varPlayer, teamnum )
    local tips,result

	result = 1

    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_BUS )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于bus状态"
	end

	return result,tips
end




function x700026_CheckAllMemberErrorState_Enjail( varMap, varPlayer, teamnum )
    local tips,result

	result = 1


    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_ENJAIL )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于监禁状态"
	end

	return result,tips
end





function x700026_CheckAllMemberErrorState_Wedding( varMap, varPlayer, teamnum )
    local tips,result

	result = 1


    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_WEDDING )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于婚礼状态"
	end

	return result,tips
end





function x700026_CheckAllMemberErrorState_Tongqu( varMap, varPlayer, teamnum )
    local tips,result

	result = 1


    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_TONGQUBUFF )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于童趣状态"
	end

	return result,tips
end





function x700026_CheckAllMemberErrorState_Tiesan( varMap, varPlayer, teamnum )
    local tips,result

	result = 1


    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_TIESAN )
        if varState == 1 then
            if result == 1 then
                result = 0
                tips = GetName(varMap, memberId)
            else
                tips = tips..","..GetName(varMap, memberId)
            end
        end
    end

	if result == 0 then
		tips = tips.." 处于铁人三项状态"
	end

	return result,tips
end




function x700026_CheckAllMemberErrorState_FubenReady( varMap, varPlayer, teamnum )
    local tips,result

	result = 1


    
    for varI = 0, teamnum - 1 do
        local memberId = GetNearTeamMember(varMap, varPlayer, varI);
        local varState = IsPlayerMutexState(varMap,memberId,PLAYER_STATE_READYFUBEN )
        if varState == 1 then
            if result == 1 then
                result = 0
                
            else
                
            end
			break;
        end
    end

	if result == 0 then
		tips = "您已经申请进入一个副本，请耐心等待"
	end

	return result,tips
end


function x700026_ShowTipsToAll(varMap, varStr)

	local humancount = GetFuben_PlayerCount(varMap);

	
	for varI = 0, humancount - 1 do
		local humanId = GetFuben_PlayerObjId(varMap, varI);
		
		StartTalkTask(varMap);
			TalkAppendString(varMap,varStr);
		StopTalkTask();
		DeliverTalkTips(varMap, humanId);
	end
end





function x700026_CreateNpc( varMap)
	
	 local varRet = 0;
	 if x700026_var_GameNPC.title~="" then
		varRet = CreateMonster(varMap, x700026_var_GameNPC.type, x700026_var_GameNPC.varX, x700026_var_GameNPC.z, 3, 0, -1,x700026_var_GameNPC.guid, -1,-1,x700026_var_GameNPC.facedir, "",x700026_var_GameNPC.title )	
	 else
		varRet = CreateMonster(varMap, x700026_var_GameNPC.type, x700026_var_GameNPC.varX, x700026_var_GameNPC.z, 3, 0, -1,x700026_var_GameNPC.guid, -1,-1,x700026_var_GameNPC.facedir)	
	 
	 end

	 SetFubenData_Param(varMap, x700026_CSP_NPC_ID, varRet )
end




function x700026_InitGrowXZ(varMap)
	local posList ={}

	for varI=1, 34 do
		local j= random(1,x700026_var_GrowPosLen)
		
		while 1>0 do
			local bDup =0;
			for k=1, varI-1 do
				if posList[k] == j then
					j= random(1,x700026_var_GrowPosLen)
					bDup = 1;
					break;
				end
			end

			if bDup==0 then
				break;
			
			end
			
		end
		posList[varI] = j;
		SetFubenData_Param(varMap, x700026_CSP_GP_POSIDX_START+varI,j)
	end
end


function x700026_InitMyGrowXZ(varMap, iIndex)

	local j= random(1,x700026_var_GrowPosLen)
		
	while 1>0 do
		local bDup =0;
		for k=1, 34 do
			local nStatus = GetFubenData_Param(varMap, x700026_CSP_GP_BOXID_START+k);

			if nStatus>=0 or nStatus==-1 then
				local nPosIndex= GetFubenData_Param(varMap, x700026_CSP_GP_POSIDX_START+k)

				if nPosIndex==j then
					j= random(1,x700026_var_GrowPosLen)
					bDup = 1;
					break;
				end
			end
			
		end

		if bDup==0 then
			break;
		
		end
		
	end
	
	SetFubenData_Param(varMap, x700026_CSP_GP_POSIDX_START+iIndex,j)

end


function x700026_ProcCreateGrowPoint(varMap,growPointType,tx,ty)
	
	local varBoxId = ItemBoxEnterScene(tx, ty, growPointType, varMap, 0, -1)
	
	SetGrowPointObjID(varMap,growPointType,tx,ty,varBoxId)
	
	

	SetFubenData_Param(varMap, x700026_CSP_GP_BOXID_START+growPointType-x700026_var_Start_GPID, varBoxId);
	SetFubenData_Param(varMap, x700026_CSP_GP_ID_START+growPointType-x700026_var_Start_GPID, growPointType);	
	
end

function x700026_ProcGpCreate(varMap,growPointType,varX,varY)
	
	x700026_ProcCreateGrowPoint(varMap,growPointType,varX,varY)	
	
end



function x700026_ProcGpOpen(varMap,varPlayer,varTalknpc)
	
	return 0;
	
end

function x700026_ProcGpRecycle(varMap,varPlayer,varTalknpc)
	
	return 1;
	
end

function x700026_ProcGpProcOver(varMap,varPlayer,varTalknpc)
	
	
	
	local nGrowpointId = 0;
	for varI=1, 34 do
		local varBoxId = GetFubenData_Param(varMap, x700026_CSP_GP_BOXID_START+varI);
		if varBoxId== varTalknpc then
			nGrowpointId = GetFubenData_Param(varMap, x700026_CSP_GP_ID_START+varI);
			local strPlayerName = GetName( varMap,varPlayer)
			SetFubenData_Param(varMap, x700026_CSP_GP_BOXID_START+varI,-2);
			SetFubenData_Param(varMap, x700026_CSP_GP_ID_START+varI,-1);
			local strPaiName = "";
			if nGrowpointId-x700026_var_Start_GPID>0 and nGrowpointId-x700026_var_Start_GPID<=34 then
				strPaiName = x700026_var_Pai_NameList[nGrowpointId-x700026_var_Start_GPID].varName
			else

			end
			local varStr = strPlayerName.."采集了"..strPaiName
			x700026_ShowTipsToAll(varMap, varStr)
			
			break;
		end
	end

	
	SetGrowPointIntervalContainer(varMap,nGrowpointId,x700026_var_GPInterval2)

	x700026_ProcAnwer(varMap,nGrowpointId)
	

	
end


function x700026_OpenCheck(varMap,varPlayer,AbilityId,AblityLevel)

	
end





function x700026_ProcAreaEntered( varMap, varPlayer, varArea, varQuest )
end




function x700026_ProcAreaLeaved( varMap, varPlayer, varScript, varQuest )
end








