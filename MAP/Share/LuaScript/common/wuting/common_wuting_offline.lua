x300178_var_FileId 				= 300178
x300178_var_Name                  = "【美】大都美丽会"

x300178_var_BaseBuffId        = 7646

x300178_var_XPBuffId          = 7647

x300178_var_DoubleXPBuffId    = 7648

x300178_var_CreditBuffId      = 7649

x300178_var_DoubleCreditBuffId= 7650

x300178_var_4ManaBuffId       = 7644

x300178_var_8ManaBuffId       = 7645

x300172_var_10H8XP            = 7667

x300172_var_10H8Credit        = 7666

x300172_var_10H8Mana          = 7665
x300178_var_BuffTable         = {
                                { buffId = 7647, xp = 4,   credit = 0, mana = 0, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 45, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },																							 
                                                                                           } }, 
                                { buffId = 7648, xp = 8,   credit = 0, mana = 0, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 45, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },	
                                                                                           } }, 
                                { buffId = 7649, xp = 0,   credit = 4, mana = 0, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },
                                                                                           } }, 
                                { buffId = 7650, xp = 0,   credit = 8, mana = 0, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },
                                                                                           } }, 
                                { buffId = 7644, xp = 0,   credit = 0, mana = 4, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },
                                                                                           } }, 
                                { buffId = 7645, xp = 0,   credit = 0, mana = 8, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },
                                                                                           } }, 
                                { buffId = 7667, xp = 8,   credit = 0, mana = 0, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 45, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },	
                                                                                           } }, 
                                { buffId = 7666, xp = 0,   credit = 8, mana = 0, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },
                                                                                           } }, 
                                { buffId = 7665, xp = 0,   credit = 0, mana = 8, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },
                                                                                           } }, 
                                { buffId = 7668, xp = 4,   credit = 0, mana = 0, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 45, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },	
                                                                                           } }, 
                                { buffId = 7669, xp = 0,   credit = 4, mana = 0, coffs = { { xpCoff = 12.5, creCoff = 1, manaCoff = 1, minLevel = 40, maxLevel = 70 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 70, maxLevel = 80 },
                                                                                             { xpCoff = 22.5, creCoff = 1, manaCoff = 1, minLevel = 80, maxLevel = 1000 },
                                                                                           } }, 
}

x300178_var_strNotEnoughMoney = "您身上的现银（银卡）不足，不能使用离线托管模块"



x300178_var_CloseTime         = 540

x300178_var_OpenTime          = 1380

x300178_var_AdditionalLevel   = 90

function x300178_ProcEnumEvent( varMap, varPlayer, varTalknpc, varQuest)
    
    local minute = GetMinOfDay()
    if minute >= x300178_var_OpenTime or minute < x300178_var_CloseTime - 1 then
        TalkAppendButton( varMap, x300178_var_FileId, "【美丽会】离线托管", 3, 1)
    end
    
    
    
end


function x300178_ProcEventEntry( varMap, varPlayer, varTalknpc, idScript, idExt)
    
    local varLevel = GetLevel( varMap, varPlayer)
    local xpAddition = 1
    if varLevel >= x300178_var_AdditionalLevel then
        xpAddition = 1.3
    end
    local lvCoff = 12.5
    if varLevel >= 70 and varLevel < 80 then
        lvCoff = 22.5
    elseif varLevel >= 80 then
        lvCoff = 45
    end
    local varIndex = 1
    for varI, item in x300178_var_BuffTable[ 1].coffs do
        if varLevel >= item.minLevel and varLevel < item.maxLevel then
            varIndex = varI
        end
    end
    
    local currentTime = GetMinOfDay()
    local remain = x300178_var_CloseTime - currentTime
    if currentTime > x300178_var_CloseTime then
        remain = x300178_var_CloseTime + 1440 - currentTime
    end

    remain = remain * 6
    
    local highXp = 0
    local highCredit = 0
    StartTalkTask( varMap)
        TalkAppendString( varMap, "#Y"..x300178_var_Name)
        TalkAppendString( varMap, "\t欢迎您使用离线托管服务， 在本次美丽会关闭前最多可得的奖励如下：#r" )
        for varI, item in x300178_var_BuffTable do
            if IsHaveSpecificImpact( varMap, varPlayer, item.buffId) == 1 then
            		local baseBuff = 0
                local amountTime = GetImpactContinuanceByDataIndex( varMap, varPlayer, item.buffId)
                if amountTime > 0 then
                    local remainTime = amountTime - GetImpactContinuanceElapsed( varMap, varPlayer, item.buffId)
                    local times = remainTime / 10000
                    
                    local reward = item.xp * item.coffs[ varIndex].xpCoff * varLevel * xpAddition + item.credit + item.mana
                    local allTimes = 0
                    if reward > 0 then
                        if times > remain then
                            allTimes = remain
                        else
                            allTimes = times
                            baseBuff = remain - times
                        end
                        if item.xp ~= 0 then
                            TalkAppendString( varMap, format( "\t#Y经验：%d", reward * allTimes + baseBuff * varLevel * lvCoff * xpAddition) )
                        end
                        if item.credit ~= 0 then
                            TalkAppendString( varMap, format( "\t#Y声望：%d", reward * allTimes + baseBuff) )
                        end
                        if item.mana ~= 0 then
                            TalkAppendString( varMap, format( "\t#Y威望：%d#r", reward * allTimes) )
                        end
                    end
                    if item.xp ~= 0 then
                        highXp = 1
                    end
                    if item.credit ~= 0 then
                        highCredit = 1
                    end
                end
            end
        end
        if highXp == 0 then
            local amountTime = GetImpactContinuanceByDataIndex( varMap, varPlayer, x300178_var_BaseBuffId)
            if amountTime > 0 then
                local remainTime = amountTime - GetImpactContinuanceElapsed( varMap, varPlayer, x300178_var_BaseBuffId)
                local times = remainTime / 10000
                local allTimes = 0
                if times > remain then
                    allTimes = remain
                else
                    allTimes = times
                end
                TalkAppendString( varMap, format( "\t#Y经验：%d#r", varLevel * allTimes * lvCoff *xpAddition) )
            end
        end
        if highCredit == 0 then
            local amountTime = GetImpactContinuanceByDataIndex( varMap, varPlayer, x300178_var_BaseBuffId)
            if amountTime > 0 then
                local remainTime = amountTime - GetImpactContinuanceElapsed( varMap, varPlayer, x300178_var_BaseBuffId)
                local times = remainTime / 10000
                local allTimes = 0
                if times > remain then
                    allTimes = remain
                else
                    allTimes = times
                end
                TalkAppendString( varMap, format( "\t#Y声望：%d#r", allTimes) )
            end
        end
		local varLevel = GetLevel(varMap, varPlayer)
		local money = varLevel*200
		local ding=0
		local liang=0
		local wen=0
		local varLevel = GetLevel(varMap, varPlayer)
		local money = floor( varLevel*200)
		ding = floor( money / (1000*1000) )
		liang = floor( mod(money,1000*1000)/ 1000 )
		wen = mod(money,1000)
		local varReadme = format("\t#G当您选择使用#R离线托管#G模块，您需要花费#R%d锭%d两%d文#W现银（银卡）", ding,liang,wen);
		TalkAppendString( varMap,varReadme )
        TalkAppendString( varMap, "\t#G如果您选择确定，您将会马上离线，并且在您上线时会得到您#R离线时间内#G的收益，您需要现在离线托管吗？#r" )
		TalkAppendString( varMap, "\t#G小提示：您的离线时间最好大于您身上的跳舞状态时间，就可以获得最理想的收益。" )
    StopTalkTask()
    DeliverTalkInfo( varMap, varPlayer, varTalknpc, x300178_var_FileId, -1)
end


function x300178_ProcMapPlayerNewConnectEnter( varMap, varPlayer)
    if IsInitBecauseReconnect( varMap, varPlayer) == 1 then
        if GetPlayerGameData( varMap, varPlayer, MD_WUTING_OFFLINE_FLAG[ 1], MD_WUTING_OFFLINE_FLAG[ 2], MD_WUTING_OFFLINE_FLAG[ 3] ) == 1 then
            
            KickPlayer( varMap, varPlayer, 0)
        end
        return
    end

    local olDate = GetPlayerGameData( varMap, varPlayer, MD_WUTING_OFFLINE_DATE[ 1], MD_WUTING_OFFLINE_DATE[ 2], MD_WUTING_OFFLINE_DATE[ 3] )
    local olTime = GetPlayerGameData( varMap, varPlayer, MD_WUTING_OFFLINE_TIME[ 1], MD_WUTING_OFFLINE_TIME[ 2], MD_WUTING_OFFLINE_TIME[ 3] )

    local varLevel = GetLevel( varMap, varPlayer)
    local xpAddition = 1
    if varLevel >= x300178_var_AdditionalLevel then
        xpAddition = 1.3
    end
    local lvCoff = 12.5
    if varLevel >= 70 and varLevel < 80 then
        lvCoff = 22.5
    elseif varLevel >= 80 then
        lvCoff = 45
    end

    local varIndex = 1
    for varI, item in x300178_var_BuffTable[ 1].coffs do
        if varLevel >= item.minLevel and varLevel < item.maxLevel then
            varIndex = varI
        end
    end

    
    local times = 3600
    local onDate = GetDayOfYear()
    local onTime = GetMinOfDay()
    
    if olDate == 500 then olDate = 0 end
    if onDate == 500 then onDate = 0 end
    
    if onDate == olDate then
        
        if onTime >= x300178_var_OpenTime then
            
            if olTime >= x300178_var_OpenTime then
                
                times = ( onTime - olTime) * 6
            else
                
                times = ( onTime - x300178_var_OpenTime + x300178_var_CloseTime - olTime) * 6
            end
        else
            
            if onTime >= x300178_var_CloseTime then
                
                times = ( x300178_var_CloseTime - olTime) * 6
            else
                
                times = ( onTime - olTime) * 6
            end
        end
    else
        
        local days = onDate - olDate
        if olDate > onDate then
            
            local varY,m,d = GetYearMonthDay()
            if mod( varY, 4) == 0 then
                
                days = onDate + 366 - olDate
            else
                days = onDate + 365 - olDate
            end
        end
        if days == 1 and onTime < olTime then
            
            if onTime >= x300178_var_OpenTime then
                
                if olTime >= x300178_var_OpenTime then
                    
                    times = 600 - ( olTime - onTime)
                else
                    
                    times = onTime - x300178_var_OpenTime + x300178_var_CloseTime - olTime
                end
            else
                
                if onTime >= x300178_var_CloseTime then
                    
                    times = x300178_var_CloseTime + 1440 - olTime
                else
                    
                    if olTime >= x300178_var_OpenTime then
                        
                        times = onTime + 1440 - olTime
                    else
                        
                        times = onTime + x300178_var_CloseTime - olTime + 60
                    end
                end
            end
            times = times * 6
        end
    end

    
    local strLog = format( "WTL: Player %s who has offline management enter game with %d offline times. ", GetName( varMap, varPlayer), times)
    WriteLog( 1, strLog)

    if times == 0 then
        
        SetPlayerGameData( varMap, varPlayer, MD_WUTING_OFFLINE_FLAG[ 1], MD_WUTING_OFFLINE_FLAG[ 2], MD_WUTING_OFFLINE_FLAG[ 3], 0)

        return
    end

    
    

    local highXp = 0
    local highCredit = 0
    local baseReward = 0
    local baseXP = 0
    local baseCredit = 0
    local xpAmount = 0
    local creditAmount = 0
    local manaAmount = 0
    local temp = GetImpactContinuanceByDataIndex( varMap, varPlayer, x300178_var_BaseBuffId)
    local baseRemain = 0
    if temp > 0 then
        baseRemain = temp - GetImpactContinuanceElapsed( varMap, varPlayer, x300178_var_BaseBuffId)
        baseRemain = baseRemain / 10000
    end
    for varI, item in x300178_var_BuffTable do
        if IsHaveSpecificImpact( varMap, varPlayer, item.buffId) == 1 then
            local amountTime = GetImpactContinuanceByDataIndex( varMap, varPlayer, item.buffId)
            if amountTime > 0 then
                local remainTime = amountTime - GetImpactContinuanceElapsed( varMap, varPlayer, item.buffId)
                local buffTimes = remainTime / 10000
                local strLog = format( "WTL: Player %s remain buff( varId: %d, times: %d) when connection. ", GetName( varMap, varPlayer), item.buffId, buffTimes)
                WriteLog( 1, strLog)
                
                local reward = item.xp * item.coffs[ varIndex].xpCoff * varLevel * xpAddition + item.credit + item.mana
                local finalReward = 0
                local remainBuff = 0
                
                baseReward = 0
                if buffTimes > times then   
                    
                    finalReward = times * reward
                    
                    remainBuff = ( buffTimes - times) * 10000
                else    
                    
                    finalReward = buffTimes * reward
                    if baseRemain > buffTimes then
                        
                        if baseRemain > times then
                            baseReward = times - buffTimes
                        else
                            baseReward = baseRemain - buffTimes
                        end
                    end
                end
                if item.xp ~= 0 then
                    AddExp( varMap, varPlayer, finalReward)
                    WriteLog( 1, format( "WTL: Add %d varExp with buff %d. ", finalReward, item.buffId) )
                    highXp = 1
                    baseXP = baseReward * lvCoff * varLevel * xpAddition
                    xpAmount = xpAmount + finalReward
                elseif item.credit ~= 0 then
                    SetShengWang( varMap, varPlayer, GetShengWang( varMap, varPlayer) + finalReward)
                    highCredit = 1
                    baseCredit = baseReward
                    creditAmount = creditAmount + finalReward
                elseif item.mana ~= 0 then
                    SetPlayerGoodBadValue( varMap, varPlayer, GetPlayerGoodBadValue( varMap, varPlayer) + finalReward)
                    manaAmount = manaAmount + finalReward
                end
                
                SetImpactContinuanceByDataIndex( varMap, varPlayer, item.buffId, remainBuff)
                WriteLog( 1, format( "WTL: Refresh Buff(varId:%d) time is %d ms. ", item.buffId, remainBuff) )
                ResetImpactContinuanceElapsed( varMap, varPlayer, item.buffId)
                RefreshImpactByDataIndex( varMap, varPlayer, item.buffId)
            end
        end
    end

    local amountTime = GetImpactContinuanceByDataIndex( varMap, varPlayer, x300178_var_BaseBuffId)
    local remainTime = amountTime - GetImpactContinuanceElapsed( varMap, varPlayer, x300178_var_BaseBuffId)
    local buffTimes = remainTime / 10000
    if times >= buffTimes then
        
        CancelSpecificImpact( varMap, varPlayer, x300178_var_BaseBuffId)
    else
        
        SetImpactContinuanceByDataIndex( varMap, varPlayer, x300178_var_BaseBuffId, ( buffTimes - times) * 10000)
        ResetImpactContinuanceElapsed( varMap, varPlayer, x300178_var_BaseBuffId)
        RefreshImpactByDataIndex( varMap, varPlayer, x300178_var_BaseBuffId)
    end
    
    if highXp == 0 then
        if amountTime > 0 then
            local finalReward = 0
            if buffTimes > times then
                finalReward = times * lvCoff * varLevel * xpAddition
            else
                finalReward = buffTimes * lvCoff * varLevel * xpAddition
            end
            AddExp( varMap, varPlayer, finalReward)
            xpAmount = xpAmount + finalReward
            WriteLog( 1, format( "WTL: Add %d varExp only by base Buff. ", finalReward) )
        end
    end
    if baseXP > 0 then
        AddExp( varMap, varPlayer, baseXP)
        xpAmount = xpAmount + baseXP
        WriteLog( 1, format( "WTL: Add %d varExp by remain base Buff. ", baseXP) )
    end
    
    if highCredit == 0 then
        if amountTime > 0 then
            local finalReward = 0
            if buffTimes > times then
                finalReward = times
            else
                finalReward = buffTimes
            end
            SetShengWang( varMap, varPlayer, GetShengWang( varMap, varPlayer) + finalReward)
            creditAmount = creditAmount + finalReward
            WriteLog( 1, format( "WTL: Add %d sw only by base Buff. ", finalReward) )
        end
    end
    if baseCredit > 0 then
        SetShengWang( varMap, varPlayer, GetShengWang( varMap, varPlayer) + baseCredit)
        creditAmount = creditAmount + baseCredit
        WriteLog( 1, format( "WTL: Add %d varExp by remain base Buff. ", baseCredit) )
    end

    
    local strReward = "您在离线托管时间内获得的奖励如下：#G#r"
    local strDialog = "\t#G感谢您使用本店的离线托管服务，在您离线的这段时间您总共获得的奖励如下：#r #r"
    if xpAmount > 0 then
        strReward = strReward..format( "经验：%d点#r", xpAmount)
        strDialog = strDialog..format( "经验：%d点#r #r", xpAmount)
    end
    if creditAmount > 0 then
        strReward = strReward..format( "声望：%d点#r", creditAmount)
        strDialog = strDialog..format( "声望：%d点#r #r", creditAmount)
    end
    if manaAmount > 0 then
        strReward = strReward..format( "威望：%d点#r", manaAmount)
        strDialog = strDialog..format( "威望：%d点#r #r", manaAmount)
    end
    SendNormalPost( varMap, varPlayer, GetName( varMap, varPlayer), strDialog)
    LuaScenceM2Player( varMap, varPlayer, strReward, GetName( varMap, varPlayer), 1, 1)

    
    SetPlayerGameData( varMap, varPlayer, MD_WUTING_OFFLINE_FLAG[ 1], MD_WUTING_OFFLINE_FLAG[ 2], MD_WUTING_OFFLINE_FLAG[ 3], 0)
end


function x300178_ProcAcceptCheck( varMap, varPlayer, varTalknpc)
    
    
    local minute = GetMinOfDay()
    if minute < x300178_var_CloseTime and minute >= x300178_var_CloseTime - 1 then
        Msg2Player( varMap, varPlayer, "美丽会关闭前1分钟不能进行离线托管", 8, 3)
        return
    end

    
	local varLevel = GetLevel(varMap, varPlayer)
	local money = varLevel*200
    if GetGoldMode( varMap, varPlayer ) == 1 then
        if CostMoney( varMap, varPlayer, 0, money,306) ~= 1 then
            if CostMoney(varMap, varPlayer, 1, money,306) ~= 1 then
                Msg2Player( varMap, varPlayer, x300178_var_strNotEnoughMoney.."。", 8, 2)
                Msg2Player( varMap, varPlayer, x300178_var_strNotEnoughMoney, 8, 3)
                return
            end
        end
    else
        if CostMoney( varMap, varPlayer, 1, money,306) ~= 1 then
            if CostMoney(varMap, varPlayer, 0, money,306) ~= 1 then
                Msg2Player( varMap, varPlayer, x300178_var_strNotEnoughMoney.."。", 8, 2)
                Msg2Player( varMap, varPlayer, x300178_var_strNotEnoughMoney, 8, 3)
                return
            end
        end
    end

    
    local strLog = format( "WTL: Player %s use offline management with sw(%d), ww(%d), xp=", GetName( varMap, varPlayer), GetShengWang( varMap, varPlayer), GetPlayerGoodBadValue( varMap, varPlayer) )
    strLog = strLog..GetExp( varMap, varPlayer)
    WriteLog( 1, strLog)

		
		LuaCallNoclosure( 203830, "Finishlixian", varMap, varPlayer)

    for varI, item in x300178_var_BuffTable do
        if IsHaveSpecificImpact( varMap, varPlayer, item.buffId) == 1 then
            local amountTime = GetImpactContinuanceByDataIndex( varMap, varPlayer, item.buffId)
            if amountTime > 0 then
                local remainTime = amountTime - GetImpactContinuanceElapsed( varMap, varPlayer, item.buffId)
                strLog = format( "WTL: With Buff %d remain buff time %d second. ", item.buffId, floor( remainTime / 1000) )
                WriteLog( 1, strLog)
            end
        end
    end
    if IsHaveSpecificImpact( varMap, varPlayer, x300178_var_BaseBuffId) == 1 then
        local amountTime = GetImpactContinuanceByDataIndex( varMap, varPlayer, x300178_var_BaseBuffId)
        if amountTime > 0 then
            local remainTime = amountTime - GetImpactContinuanceElapsed( varMap, varPlayer, x300178_var_BaseBuffId)
            strLog = format( "WTL: With Base Buff remain buff time %d second. ", floor( remainTime / 1000) )
            WriteLog( 1, strLog)
        end
    end

    
    SetPlayerGameData( varMap, varPlayer, MD_WUTING_OFFLINE_DATE[ 1], MD_WUTING_OFFLINE_DATE[ 2], MD_WUTING_OFFLINE_DATE[ 3], GetDayOfYear() )
    
    SetPlayerGameData( varMap, varPlayer, MD_WUTING_OFFLINE_TIME[ 1], MD_WUTING_OFFLINE_TIME[ 2], MD_WUTING_OFFLINE_TIME[ 3], GetMinOfDay() )
    
    SetPlayerGameData( varMap, varPlayer, MD_WUTING_OFFLINE_FLAG[ 1], MD_WUTING_OFFLINE_FLAG[ 2], MD_WUTING_OFFLINE_FLAG[ 3], 1)
    
    KickPlayer( varMap, varPlayer, 1)
    
    SetKickTime( varMap, varPlayer, 11000)

    SetPlayerRuntimeData( varMap, varPlayer, RD_GUJI_SCENEID, 1)
end


